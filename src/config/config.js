const config = {
  IDLE_TIMEOUT: 5 * 60 * 1000, // 5 minutes in milliseconds
  MAX_QUEUE_DISPLAY: 10, // Tracks shown per queue page
  DEFAULT_VOLUME: 50, // 0-100
  SEARCH_RESULTS: 5, // Number of search results to show
};

module.exports = config;
